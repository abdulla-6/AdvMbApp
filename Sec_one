import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: const HomePage(),
    );
  }
}

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Main Page")),
      body: GestureDetector(
        onTap: () {
          Navigator.push(
            context,
            MaterialPageRoute(builder: (context) => const DetailClass()),
          );
        },
        // Using Align with Alignment.topLeft places it at the top left
        child: const Align(
          alignment: Alignment.topLeft,
          child: Padding(
            padding: EdgeInsets.all(16.0),
            child: Hero(
              tag: 'hero-tag',
              child: FlutterLogo(size: 100),
            ),
          ),
        ),
      ),
    );
  }
}

class DetailClass extends StatelessWidget {
  const DetailClass({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: GestureDetector(
        onTap: () => Navigator.pop(context),
        // Using Center places it in the middle of the screen
        child: const Center(
          child: Hero(
            tag: 'hero-tag',
            child: FlutterLogo(size: 200),
          ),
        ),
      ),
    );
  }
}
